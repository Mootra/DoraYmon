qqbot:
  appid: ""
  secret: ""
  sandbox: true

deepseek:
  api_key: ""
  base_url: "https://api.deepseek.com"
  model: "deepseek-v4-flash"
  temperature: 0.7

bot:
  command_prefix: "/"
  admin_openids: []

food:
  natural_trigger: true

chat:
  history_enabled: false
  history_limit: 10
  history_max_content_length: 1000

log:
  level: "INFO"

paths:
  data_dir: "data"
  log_dir: "logs"
