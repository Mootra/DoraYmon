# 短期聊天上下文

短期上下文默认关闭，通过 `BOT_ENABLE_CHAT_HISTORY=true` 开启。它只保存当前私聊用户或当前“群 + 用户”会话的近期完整问答轮次，不是长期记忆。

`/清空上下文` 只清除当前会话，`/上下文状态` 显示开关、消息数量和字符预算，`/上下文摘要` 返回受限的本地节选。

会话闲置时间由 `BOT_CHAT_CONTEXT_TTL_MINUTES` 控制。用户以“换个话题”“忽略之前的对话”或“重新开始”等明确表达开头时，旧会话会被清空。
