# 本地知识库 RAG

管理员把 UTF-8 Markdown 或 TXT 文件放入知识目录，再运行 `scripts/index_knowledge.py` 离线重建 SQLite FTS5 索引。

`/知识问` 是严格知识问答入口，只能依据检索资料回答；没有可靠资料时应明确拒答。`/知识来源` 用于单独查看检索到的来源，`/知识库状态` 查看文档数和分块数。

普通聊天开启 RAG 后会尝试可选知识增强；没有命中资料时仍继续普通对话。当前实现是 BM25 关键词检索，不是向量数据库。
